package postgres

// Repository implementations are split per domain into their own files
// (auth_repo.go, agents_repo.go, inventory_repo.go, audit_repo.go) once
// Phase 1 lands. This file is reserved as the package's table of contents
// so that grep / IDE navigation has a clear entry point.
